import React from 'react';
import { SandraImages } from '../lib/sandra-images';

export default function WelcomeEditorial() {
  // This component is no longer used since the "But Wait... AI Photos?" section
  // has been moved to the main editorial landing page
  return null;
}
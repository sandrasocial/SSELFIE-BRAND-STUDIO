// Re-export lazy-wrapped components
export {
  LazyDialog as Dialog,
  LazyCommand as Command,
  LazySheet as Sheet,
  LazyAlertDialog as AlertDialog,
  LazyButton as Button,
  LazyToaster as Toaster,
  LazyTooltipProvider as TooltipProvider,
  withSuspense
} from './lazy-wrapper';
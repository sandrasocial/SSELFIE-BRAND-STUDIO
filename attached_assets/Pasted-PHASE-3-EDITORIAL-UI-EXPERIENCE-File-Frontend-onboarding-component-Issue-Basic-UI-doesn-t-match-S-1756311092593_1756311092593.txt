PHASE 3: EDITORIAL UI EXPERIENCE
File: Frontend onboarding component
Issue: Basic UI doesn't match Sandra's editorial style guide
ADD this editorial styling to the onboarding component:
css/* Maya Onboarding - Editorial Style matching Sandra's brand */
.maya-onboarding {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  font-weight: 300;
  color: #0a0a0a;
  background: #ffffff;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
}

.onboarding-container {
  max-width: 800px;
  margin: 0 auto;
  padding: 120px 40px;
  text-align: center;
}

.progress-indicator {
  margin-bottom: 60px;
  font-size: 11px;
  font-weight: 400;
  letter-spacing: 0.4em;
  text-transform: uppercase;
  color: #666666;
}

.maya-message {
  font-family: 'Times New Roman', serif;
  font-size: clamp(28px, 4vw, 48px);
  font-style: italic;
  letter-spacing: -0.02em;
  line-height: 1.3;
  margin-bottom: 60px;
  color: #0a0a0a;
}

.input-section textarea {
  width: 100%;
  min-height: 120px;
  padding: 30px;
  border: 1px solid #e5e5e5;
  background: #ffffff;
  font-family: inherit;
  font-size: 16px;
  line-height: 1.6;
  resize: vertical;
  border-radius: 0;
}

.quick-buttons {
  display: flex;
  flex-wrap: wrap;
  gap: 20px;
  justify-content: center;
  margin-bottom: 60px;
}

.quick-btn {
  padding: 16px 32px;
  font-size: 11px;
  font-weight: 400;
  letter-spacing: 0.3em;
  text-transform: uppercase;
  text-decoration: none;
  border: 1px solid #0a0a0a;
  color: #0a0a0a;
  background: transparent;
  transition: all 300ms ease;
  cursor: pointer;
}

.quick-btn:hover {
  background: #0a0a0a;
  color: #ffffff;
}

.continue-btn {
  padding: 20px 60px;
  font-size: 11px;
  font-weight: 400;
  letter-spacing: 0.3em;
  text-transform: uppercase;
  border: 2px solid #0a0a0a;
  color: #ffffff;
  background: #0a0a0a;
  transition: all 300ms ease;
  cursor: pointer;
}

.continue-btn:hover {
  background: transparent;
  color: #0a0a0a;
}

@media (max-width: 768px) {
  .onboarding-container {
    padding: 60px 20px;
  }
  
  .maya-message {
    font-size: clamp(24px, 6vw, 36px);
  }
  
  .quick-buttons {
    flex-direction: column;
    align-items: center;
  }
  
  .quick-btn, .continue-btn {
    width: 100%;
    max-width: 300px;
  }
}

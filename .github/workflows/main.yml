name: Legacy CI/CD (Deprecated)

# This workflow is deprecated in favor of ci.yml
# Keeping for reference during transition period

on:
  workflow_dispatch: # Only run manually to avoid conflicts
  
jobs:
  legacy-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation Notice
        run: |
          echo "⚠️  This workflow is deprecated!"
          echo "Please use the new CI/CD pipeline in ci.yml"
          echo "This workflow will be removed in a future update"